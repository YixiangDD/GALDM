# GA-LDM — pinned to the versions used for every number reported in the paper.
# Python 3.10 or 3.11.
#
# PyTorch is intentionally NOT pinned to a CUDA build here, because the correct
# wheel depends on your driver. Install it first, then this file:
#
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# CPU-only works for the --quick preset; the full protocol needs a GPU.

torch==2.5.1
numpy==1.26.2
pandas==2.1.4
scipy==1.11.4
scikit-learn==1.3.2
imbalanced-learn==0.13.0
xgboost==3.2.0
matplotlib==3.8.2
umap-learn==0.5.5
openpyxl==3.1.5

# Only needed by paper_figures/*_to_pptx.py, which export the figures as
# fully editable PowerPoint decks. Everything else runs without it.
python-pptx==1.0.2
